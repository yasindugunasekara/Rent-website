# Root-level secrets — never commit the real .env, only .env.example
.env
.env.local
.env.*.local

# OS/editor noise
.DS_Store
Thumbs.db
*.swp

# Node (also covered by web/.gitignore, repeated here for anything run from
# the repo root)
node_modules/
.next/
